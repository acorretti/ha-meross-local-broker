#include <tunables/global>

profile 8b8632af_meross_local_broker flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # dBus
  dbus (send, receive)
  	bus=system
	peer=(label=unconfined),
}
